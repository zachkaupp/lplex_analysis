## Record p-values for each analyte between groups

# make settings to select the two groups to compare against each other
# output format in csv: analyte, p-value
# print the two groups that are being compared


cat(cyan("Process complete\n"))
